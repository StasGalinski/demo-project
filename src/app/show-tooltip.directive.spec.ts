import { ShowTooltipDirective } from './show-tooltip.directive';

describe('ShowTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
